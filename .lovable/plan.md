## Plan: Create empty ABP project

1. Update `src/routes/index.tsx` — replace placeholder with a minimal empty page showing "ABP".
2. Update `src/routes/__root.tsx` `head()` metadata: title "ABP", description "ABP".

That's it — leaves a clean slate for you to fill in.